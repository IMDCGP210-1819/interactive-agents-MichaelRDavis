#pragma once

/**
 * Defines a navigation path connecting to nodes.
 */
class NavPath
{
public:
	/** Default constructor. */
	NavPath();

	/** Default destructor. */
	virtual ~NavPath();

private:

};